sudo rm -rf /usr/lib/aarch64-linux-gnu/libcudnn*
sudo rm -rf /usr/lib/aarch64-linux-gnu/libcublas*
sudo rm -rf /usr/lib/aarch64-linux-gnu/libcurand*
sudo rm -rf /usr/lib/aarch64-linux-gnu/libcufft*


sudo apt update
sudo apt install nvidia-jetpack

python3 -c "import torch; print(torch.__version__, torch.cuda.is_available())"

dpkg-query --show nvidia-jetpack
